# Environment Variables

This file documents the environment variables required for the admin_side module.

## Required Environment Variables

### GraphQL API
```bash
# GraphQL API endpoint
MY_CMS_API_URL=http://localhost:4000
```

### Authentication (To be configured)
```bash
# Authentication configuration (will be implemented in Phase 8)
# NEXTAUTH_URL=http://localhost:3002
# NEXTAUTH_SECRET=your-secret-key
```

### Upload/Storage (Optional)
```bash
# File upload endpoint
UPLOAD_URL=http://localhost:4000/upload
```

### Cache Configuration (Optional)
```bash
# Enable homepage cache (optional)
HOME_PAGE_CACHE_ENABLED=false
```

## Development Setup

1. Copy this file to `.env.local` in the admin_side directory
2. Update the values according to your environment
3. Never commit `.env.local` to version control

## Example .env.local

```bash
MY_CMS_API_URL=http://localhost:4000
HOME_PAGE_CACHE_ENABLED=false
```

## Notes

- The GraphQL API URL is required for the admin features to work
- Authentication environment variables will be configured in Phase 8
- Make sure your backend GraphQL server is running before starting the admin_side module
