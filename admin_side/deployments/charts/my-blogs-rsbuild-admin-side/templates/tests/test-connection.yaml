apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "admin-side.fullname" . }}-test-connection"
  labels:
    {{- include "admin-side.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "admin-side.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
